services:
  mdcng-bot:
    image: ghcr.io/imshonechen/mdcng-bot:latest
    build: .
    container_name: mdcng-bot
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
