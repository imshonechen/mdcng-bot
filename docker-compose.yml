services:
  mdcng-bot:
    image: ghcr.io/imshonechen/mdcng-bot:latest
    build: .
    container_name: mdcng-bot
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - CHAT_ID=${CHAT_ID}
